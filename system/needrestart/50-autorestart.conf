# Managed by the homeserver repo (system/needrestart/) — edit there, then `sudo system/install.sh`.
# After a library upgrade, restart affected services automatically instead of asking
# (nobody is at the console for unattended-upgrades to ask).
$nrconf{restart} = 'a';
